---

# mariadb stuck at version 5.5.x, which is NOT compatible with Magento 2.x
# Magento 2.x requires min 5.6
mariadb_packages:
    - 'MySQL-python.x86_64'

mariadb_packages_old:
    - 'mariadb.x86_64'
    - 'mariadb-libs.x86_64'
    - 'mariadb-server.x86_64'
